@model SchoolBookMVC.ViewModels.PreviewViewModel
@{
    ViewBag.Title = "Preview";
}

<h2>Preview</h2>

<blockquote>
    @*If $ax^2+bx+c=0$ with $a≠0$, then: $$x={-b±√{b^2-4ac}}/{2a}$$ $$Csc[x]={{1/{1/(Sin[x])}}*{1/{1/(Sec[x])}}}/{Cos[x]}$$*@
    \[∫_\Δd\bo ω=∫_{∂\Δ}\bo ω\]
    <br/>
    <br />
    <br />
    @using (Html.BeginForm("/HomeWorkSubmitted", "HomeWork", FormMethod.Get))
    {
        var result = Model.text;
        @Html.Raw("$$")  @result @Html.Raw("$$")
    }

</blockquote>